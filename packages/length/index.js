/**
 * Return the number of elements in a list.
 *
 * @param  {Array}  list List to get the length of.
 * @return {Number}      Number of elements in the list..
 */
export default function length (list) {
  return list.length
}
